# Canonical repo path (installer symlinks here even if you cloned elsewhere)
REPO_ROOT="/opt/streamingserver"

# Where live data (music, dbs, state) goes
DATA_ROOT="/srv/streamingserver"

# Derived paths
STACK_DIR="${DATA_ROOT}/stack"
MUSIC_LIB="${DATA_ROOT}/music/library"
MUSIC_DATA="${DATA_ROOT}/music/data"
FILEBROWSER_DB="${DATA_ROOT}/filebrowser/filebrowser.db"

# Repo-managed configs (mounted into containers; editable in git)
CADDYFILE="${REPO_ROOT}/caddy/Caddyfile"
CADDY_SITE="${REPO_ROOT}/caddy/site"
CADDY_SNIPPETS="${DATA_ROOT}/caddy/snippets"
FILEBROWSER_SETTINGS="${REPO_ROOT}/filebrowser/settings.json"
